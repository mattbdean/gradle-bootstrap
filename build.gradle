buildscript {
    ext.kotlinVersion = "0.10.4" // M10

    repositories { mavenCentral() }
    dependencies {
        classpath "org.jetbrains.kotlin:kotlin-gradle-plugin:$kotlinVersion"
    }
}

ext {
    slf4jVersion = "1.7.9"
}

subprojects {
    version = '0.0.1'
}

